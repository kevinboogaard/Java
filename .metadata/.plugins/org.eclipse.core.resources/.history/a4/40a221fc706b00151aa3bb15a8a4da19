package Main;
import javax.swing.JFrame;
import javax.swing.WindowConstants;

public class Border
{
	public Border(int _width , int _height, boolean _visible,Renderer comp )
	{
		JFrame myFrame = new JFrame("This is my frame");
		myFrame.setSize(_height,_width);
		myFrame.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
		myFrame.add(comp);
		myFrame.setVisible(_visible);
		
	}
}